{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "si",
            "database": "${workspaceFolder:si}/db.sqlite3"
        }
    ]
}